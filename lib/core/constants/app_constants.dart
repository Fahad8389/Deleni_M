class AppConstants {
  static const double walkingSpeedMps = 1.4;
  static const double coordinateToMeters = 1.0; // 100 units = 100 meters
  static const Duration pathAnimationDuration = Duration(milliseconds: 1500);
  static const Duration pulseAnimationDuration = Duration(milliseconds: 2000);
  static const double pathWidth = 7.0;
  static const double emergencyPathWidth = 10.0;
  static const double accessibilityPathWidth = 8.0;
  static const double arrowInterval = 0.3; // 30% intervals
  static const String appVersion = '1.0.0';
}
